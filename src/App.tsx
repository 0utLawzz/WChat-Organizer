import { useEffect, useMemo, useRef, useState } from "react";
export default function App() {
  return (
    <div style={{ padding: 40, fontFamily: "system-ui" }}>
      <h1>BrandEx Workspace — restoring…</h1>
      <p>Please wait for the next deploy. Full production App.tsx is being pushed.</p>
      <p>Password: brandex2026</p>
    </div>
  );
}
